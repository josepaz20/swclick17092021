function setActivacionD() {
    if (confirm("¿ Esta seguro que desea realizar ACTIVACION DIAN ?")) {
        location.href = "/sw2click/modulos/secciones/activacionD";
    }
}

function setDesactivacionD() {
    if (confirm("¿ Esta seguro que desea realizar DESACTIVACION DIAN ?")) {
        location.href = "/sw2click/modulos/secciones/desactivacionD";
    }
}
